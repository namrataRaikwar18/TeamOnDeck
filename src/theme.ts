import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontSize: 22,
    body1:{
      color:""
    },
    button: {
      textTransform: 'none'
    }
  },
});

export { theme };
